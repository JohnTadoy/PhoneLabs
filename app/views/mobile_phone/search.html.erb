<h1>Search Result</h1>
  <% @mobile.each do |m|%>
    <h2><a href="<%= mobile_phone_path(m.id) %>"><%= m.name %></a></h2>
    <img src="<%= m.image %>" alt="<%= m.name %>">
    <p><%= m.price%></p>
    <p><%= m.description %></p>
  <%end%>
</div>